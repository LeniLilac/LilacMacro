<UserControl x:Class="LilacMacro.App.Controls.OcrVisualization"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer x:Name="Viewport"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalContentAlignment="Center"
                  VerticalContentAlignment="Center"
                  PreviewMouseWheel="Viewport_OnPreviewMouseWheel"
                  PreviewMouseDown="Viewport_OnPreviewMouseDown"
                  PreviewMouseUp="Viewport_OnPreviewMouseUp"
                  MouseMove="Viewport_OnMouseMove"
                  LostMouseCapture="Viewport_OnLostMouseCapture"
                  SizeChanged="Viewport_OnSizeChanged">
        <Grid x:Name="ZoomSurface" Width="1280" Height="720">
            <Grid.LayoutTransform>
                <ScaleTransform x:Name="ZoomTransform" ScaleX="1" ScaleY="1" />
            </Grid.LayoutTransform>
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="SourceColumn" Width="*" />
                <ColumnDefinition x:Name="GapColumn" Width="14" />
                <ColumnDefinition x:Name="TextColumn" Width="*" />
            </Grid.ColumnDefinitions>
            <Border x:Name="SourcePanel" Grid.Column="0" Background="#1C202A" BorderBrush="{StaticResource InkBrush}" BorderThickness="2">
                <Viewbox Stretch="Uniform">
                    <Grid x:Name="SourceSurface" Width="1280" Height="720">
                        <Image x:Name="SourceImage" Stretch="Fill" SnapsToDevicePixels="True" />
                        <Canvas x:Name="SourceOverlay" IsHitTestVisible="False" />
                    </Grid>
                </Viewbox>
            </Border>

            <Border Grid.Column="2" Background="White" BorderBrush="{StaticResource InkBrush}" BorderThickness="2">
                <Viewbox Stretch="Uniform">
                    <Grid x:Name="TextSurface" Width="1280" Height="720" Background="White">
                        <Canvas x:Name="TextOverlay" IsHitTestVisible="False" />
                    </Grid>
                </Viewbox>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
