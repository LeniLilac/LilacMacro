<Window x:Class="LilacMacro.App.Views.UpdateConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm LilacMacro update"
        Width="440"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent"
        KeyDown="Dialog_OnKeyDown">
    <Grid Margin="8">
        <Border Style="{StaticResource CardStyle}" Padding="22,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock FontSize="22" FontWeight="Black" Text="UPDATE LILACMACRO?" />
                <TextBlock x:Name="UpdateDescription"
                           Grid.Row="1"
                           Margin="0,10,0,0"
                           FontSize="13"
                           LineHeight="19"
                           Foreground="{DynamicResource InkSoftBrush}"
                           TextWrapping="Wrap" />

                <StackPanel Grid.Row="2" Margin="0,20,0,0"
                            HorizontalAlignment="Right" Orientation="Horizontal">
                    <Button MinWidth="96" Style="{StaticResource ButtonStyle}"
                            Content="CANCEL" Click="Cancel_OnClick" />
                    <Button MinWidth="96" Margin="10,0,0,0"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Content="UPDATE" Click="Update_OnClick" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
