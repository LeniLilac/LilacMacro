# Project status

**Status: Current as of 2026-08-15.** Update this matrix in the same change that moves a capability across a boundary.

The [documentation contract](../CONTRIBUTING.md#documentation-contract) defines the status terms. In short: **Implemented** means production capability exists, **Prototype** means usable but internal or incomplete code exists, **Planned** means accepted design intent, and **Unresolved** means a decision remains open.

## Current repository

| Capability | Status | Evidence and boundary |
|---|---|---|
| Layered .NET solution | Implemented | `Core <- Windows <- Runtime <- App`, with tests and tools consuming shared lower-layer contracts |
| Dataset manifest, validation, hashing, drafts, and finalization | Implemented | Schema version 1, atomic JSON writes, collision-free final directories |
| Timed and manual Roblox frame capture | Implemented | Dataset Builder; F6 starts independent timed drafts and F5 appends manual frames |
| Frame review and annotations | Implemented | Verdicts, notes, local/global coarse boxes, per-result OCR/image relevance, exact/fuzzy matching, inferred/overridden spatial selectors, declarative required plus N-of-pool evidence, and atomic manifest updates |
| Agent dataset views | Implemented | Validation-first contact sheets, crops, OCR maps, JSONL, index, and summary |
| PP-OCRv6 detection and recognition trials | Implemented | Small/tiny pairs, CPU/GPU runtime selection, NVIDIA compute-capability detection with CUDA 11.8/12.6/12.9 routing from Pascal through Blackwell, timing, cached worker, and coordinates; Pascal CUDA 11.8 was qualified with tensor plus PP-OCRv6 small inference on a GTX 1080 Ti |
| Roblox discovery, exact client sizing, and fresh capture | Implemented | Ordinary Windows APIs and FP16 Windows Graphics Capture with display-aware HDR-to-sRGB conversion, client-only GPU crop, and tagged PNG output |
| Detector ROI capture pipeline | Implemented | Bounded client-relative regions, compact GPU atlas, one readback, deterministic grayscale conversion, and ROI-to-client match translation; Wire Test schedules diagnostic captures but unattended scheduling is absent |
| Roblox-compatible input protocol | Implemented | Bounded work-area containment/focus, acknowledged cursor motion, click, scroll, camera, and key release behavior; an oversized client fails before input rather than targeting an off-screen edge or taskbar |
| OCR normalization and layout policies | Implemented | Deterministic exact/fuzzy phrase and layout policies with automated tests; Team Swap ignores customizable names, calibrates deterministic wheel travel from a live thumb probe, waits through bounded list easing, verifies the middle landing, and uses relational Save/Load rows |
| Adaptive visual-anchor foundation | Implemented | Generic burst fingerprint builder, temporal reliability masks, bounded grayscale/edge matcher, visible Review evidence, declarative state rules, hashed PGM assets, and immutable profile revisions in Core |
| Dataset Builder and Runtime Lab split | Implemented | Dedicated launch modes construct only Capture/Review/Datasets or Debug/Wire Test/Scroll Test/Team Swap Test/Route Optimizer/Utility Tests while reusing one App/Core/Windows implementation and a cross-process Roblox-input lease; Debug includes explicit UI-scale and complete startup-settings normalizer tests, while Utility Tests exercises production Calendar Claim, Gold Shop, Raid Shop, and Expedition Shop flows |
| Browser-style Macro/Plan/Setup/Settings shell | Prototype | Current startup surface; page instances survive tab changes |
| Roblox dashboard docking | Prototype | Macro can pin a verified Roblox player into a DPI-aware `1366 x 700` live surface; a tracked post-rejoin window is repaired in place if Roblox rewrites its dock style, replacement processes reacquire when the dashboard remained exposed, and overlapping unrelated applications suspend docking until the Macro itself becomes active. Focusing standalone Roblox cannot expose or reacquire a covered dashboard. Original frame/bounds are restored on undock, tab change, minimize/occlusion, failure, or shutdown; live behavior remains owner-validated |
| Setup map gallery and placement timeline authoring | Prototype | Functional local authoring, 17 release-bundled maps with 32 reference views available to every owner/runner session plus newer compatible owner-dataset overrides, route inheritance/reset, cross-map/route setup copying with coordinate scaling, Team/Unit selectors with `1`-`6` keyboard shortcuts, explicit Place/Select cursor modes, constant-screen-size compact markers, selection-only stable-ID drag/delete actions with nearby-marker fading, wheel-burst-safe map zoom, compact placement-reference labels, centered step editors, tabs, popout, and autosave; Test Setup flushes the active route, aligns the camera, executes its complete timeline from Match Prestart, and can be canceled by the configured Macro key. Match Settings persists the 200 ms default between-upgrade settle; its remaining controls stay unwired |
| Appearance themes | Prototype | Settings applies and atomically persists a Light/Dark mode plus a tactile two-row palette grid containing ten paired solid families and ten gradient families with unique Light and Dark definitions; gradient canvases render once beneath an independent continuous-line/dot pattern, while semantic status and game-state colors remain stable |
| Debug state transitions | Prototype | Main Macro, managed sessions, and Runtime Lab share destination-first temporal transitions across primary navigation, Event, Challenge, Repeat Stage, Refuel, Route Optimizer, and Expedition Checkpoint flows. The source is checked only after the expected state fails; retained sources reacquire/retry their live action, indeterminate frames expand bounded observation without input, and scheduler recovery owns exhaustion. Saved-image-first checks retain OCR fallback/profile refresh; owner live acceptance remains required |
| Story/Raid/Challenge Wire Test | Prototype | Owner-triggered, cancellable Lobby-to-Match-Prestart navigation can run pure OCR or image-first state waits with OCR fallback/profile refresh; Challenge adds type rotation, availability/cooldown evidence, and reset-scoped eligibility; an explicit option continues through the authored placement route, Start Game, and terminal verification |
| Team scrollbar A/B and fidelity test | Prototype | Runtime Lab can independently repeat the existing midpoint drag or ramp wheel units by a per-trial increment from the top clamp, save full before/after evidence, pair each requested amount with a normalized thumb position, and write variance results; this owner test does not change production team selection |
| Randomized team swap test | Prototype | Runtime Lab can run balanced randomized Teams 1-8 through the shared production swap path, retain calibration across trials, continue after ordinary failures, and expose target, result, time, and terminal status; owner live acceptance remains required |
| Route optimizer trial test | Prototype | Runtime Lab can collect 1-1000 independent Expedition route-pool observations with production compact four-times OCR, two-observation full-pool stabilization, target-only decision fallback without profile contamination, bundled post-update D1/D2/D3 priors with local complete-pool refinement and per-device timing, selected target/threshold decisions, normalized quantities, raw text, elapsed time, and shared source/forward-state click recovery across Back, Start Game, Restart confirm, and cached Expedition Map transitions; owner live acceptance remains required |
| Fast unit playback evidence | Prototype | Quick-place batches use shared input ownership; initial Priority/Sell/DPS OCR calibrates live panel geometry and per-run compact control images, later checks require both multi-pixel Priority/Sell reference matches, and physical DPS confirmation rejects phantom selections. Two independent text-free fill samples own affordable versus stable gray; targeted Maxed OCR creates a reusable per-calibration structural image reference, and each requested press is rechecked after the configured settle |
| Deep debug diagnostics and viewer | Implemented | Main Macro, Dataset Builder, and Runtime Lab share bounded JSONL/timeline recording, redaction, crash finalization, agent contact sheets, and local ZIP retention; Main Macro/Dataset Builder retain a rolling PNG window, Runtime Lab retains its complete owner-triggered operation, and the dedicated viewer streams ZIP frames, synchronizes nearby events, and overlays client-relative click/scroll evidence |
| Privacy choices, telemetry, and diagnostic reporting | Implemented | Versioned first-run and Settings choices gate online features, fixed-schema telemetry, and default-off automatic error reports. A separate atomic consent-generation record prevents stale shared-runner settings or queued work from crossing an opt-out. Telemetry uses bounded in-memory batching with no arbitrary text or disk retry queue. Light diagnostics retain at most 32 already-acquired frames under an 80 MiB ZIP limit; active Deep Debug archives use the existing checksummed private upload path. Manual ZIP selection remains independently default-off. Production Services deployment and Backblaze staging proof remain outside this repository boundary |
| Signed Services control snapshots | Prototype | Every full Macro UI independently polls a bounded HTTPS endpoint, verifies Ed25519 signatures, freshness, monotonic revisions, and exact schemas, atomically caches one last-known-good snapshot, and applies game availability, named feature disablements, schedules, and active public codes. Production Services deployment remains outside this repository boundary |
| Active public code redemption | Prototype | At verified Lobby resets, desktop and managed-runner UIs process unattempted signed codes through separate bundled launcher/panel owners, bounded case-sensitive Windows key input, three fresh Redeem actions, and shared Areas-UI respawn cleanup; the attempted set is scoped to one user-started run and owner live acceptance remains required |
| Macro page | Prototype | Right-rail dashboard with plan selection, start/stop state, dockable Roblox surface, connected Story/Raid/Challenge/Expedition/Event plus recurring Utility scheduling, cumulative victory/defeat history, totals, and run log; exact same repeatable match tasks use verified Repeat Stage while changed/completed tasks and Challenge reset through Lobby; an exact-team cache is scoped to one uninterrupted user-started run; owner live acceptance remains required |
| Plan page | Prototype | ExpeditionsMacro-parity task fields, compact draggable task blocks with Setup-matched preview/gap/insertion feedback and deterministic adorner cleanup, finite and Forever loop blocks with live completed-run counts, global priority, and centered task/loop editors persist atomically with the selected plan; broader scheduler integration remains incomplete |
| Full Settings page | Prototype | Press-then-key bindings, Plan state, persisted mode/color themes, visible-but-DPAPI-protected private-server link, masked DPAPI webhook, Discord failure options, full/compact display policy, coordinated update controls, and the local instance manager persist; private-server Test Link uses the registered Roblox protocol and Test Webhook sends a bounded mention-free Discord message, with both outcomes surfaced through the shared bottom-right notification, while runtime-triggered Discord delivery remains unwired |
| Local instance manager | Prototype | Exact-binary TermWrap preflight/cache, loopback isolation, per-runner standard accounts/credentials/ACLs/full-UI tasks, Runner 1..16 endpoints, shared-or-isolated configuration roots, runner-only black desktop/icon policy, trusted first-launch Roblox bootstrap, legacy single-runner migration, rollback, cleanup verification, and Settings setup/add/open/remove actions exist; disposable-VM lifecycle certification and broader owner live acceptance remain required |
| Signed installer foundation | Prototype | Inno Setup and build/validation/publish scripts produce one Program Files installer plus an exact four-asset release inventory, require release signing, coordinate all-UI shutdown/relaunch, use Restart Manager for manually launched upgrades, preserve optional runner state across upgrade through repair, and block overwrite/uninstall on incomplete cleanup; no supported signed release exists |

## Accepted target architecture

| Capability | Status | Contract |
|---|---|---|
| Priority task scheduler | Prototype | Main Macro and the shared session runtime select the lowest-numbered incomplete task after each terminal result. Main Macro advances finite and Forever loop iterations, resets bounded child targets at repeating boundaries, and excludes completed finite loops. Plans are preflighted before runtime input; later operational exceptions enter an indefinite cancellable retry ladder with Roblox restart/private-server rejoin, bounded backoff, three-failure task quarantine, next-task continuation, and five-minute reconsideration. Exact same-task Story/Raid/Expedition/Event uses verified Repeat Stage; Challenge and future Tower remain one-match. Owner live acceptance and broader managed-session loop parity remain required |
| Mandatory startup normalization | Prototype | Every private-server reset closes only its Windows session's Roblox client, atomically normalizes the profile XML allowlist, rejoins, and verifies Lobby. Once per Macro start, the first Lobby also runs bounded UI-scale feedback, closes/reopens Settings, and applies the fixed positional/color-verified in-game option profile. Runtime Lab Debug exposes the same full owner test; recovery joins do not repeat the in-game passes. Owner live acceptance remains required |
| Canonical Lobby reset | Prototype | Main match runs parse the DPAPI-protected configured private-server link into a bounded `roblox://` launch target, close/reopen Roblox without a browser intermediary, wait for verified Lobby, then reevaluate priority. Exact same-task Story/Raid/Expedition/Event repetition is the bounded exception; operational recovery re-enters through this reset indefinitely until user cancellation, while every individual attempt remains bounded |
| Story runner | Prototype | Runtime Lab and main Macro share complete navigation, authored placement, and terminal handling. Exact same-task continuation clicks verified Repeat Stage, verifies Match Prestart, reruns placements, and deliberately retains the selected team and camera angle; other continuations reset through Lobby. Owner live acceptance remains required |
| Raid runner | Prototype | Runtime Lab and main Macro share complete navigation, authored placement, and terminal handling. After the final placement/configuration action, Raid Acts 2-3 periodically click the field-observed bottom-right resting area between fresh terminal checks to dismiss unit-obtainment overlays. Exact same-task continuation clicks verified Repeat Stage, verifies Match Prestart, reruns placements, and retains team/camera; owner live acceptance remains required |
| Challenge runner | Prototype | Main Macro and Runtime Lab share type rotation, random-map recognition, authored Challenge placement playback, terminal handling, Lobby return, and per-type half-hour/daily-limit eligibility; owner live acceptance remains required |
| Resource refuel runner | Prototype | Main Macro and shared local-session runtime execute Gold Mine and Resource Drill as independent recurring Utility tasks or as one combined task with separate Mine/Drill workflows and one shared interval after both complete. Lobby/Areas/Hub use fresh evidence; the relabeled three-scale owner datasets now provide a stable Add Fuel panel owner, a separate Confirm/Cancel modal owner, bounded 2/4/6-second station interaction checks, relational Min/Max selection, and bounded confirmation retries. After each station, shared Utility cleanup opens and verifies Areas before the respawn keys and verified Lobby. Consecutive Utility tasks hand off directly and a following match does not repeat startup normalization. Owner live acceptance remains required |
| Shop and Calendar utilities | Prototype | Plan tasks persist stable per-shop item selections; desktop and managed-session runtimes dynamically scan Gold/Raid/Expedition catalogs, require fresh item/Buy/availability/dialog evidence, skip unavailable rows, and schedule daily, two-day, or weekly UTC resets. Calendar derives a complete seven-card grid and clicks Day 7 through Day 1 for three verified passes. Calendar and all shops share the same verified Areas-to-respawn cleanup as refuel tasks before returning to Lobby. Owner live acceptance remains required |
| Expedition runner | Prototype | Main Macro, Runtime Lab Wire, and managed sessions share initial placements, checkpoint Continue/Extract, stable current-node handling, per-instance learned color hot path with hover/OCR fallback, per-placement Defense/Elite physical-retention replay, delayed-arrival Encounter/non-spawn Checkpoint Continue transitions, terminal handling, bounded idle reward dismissal, and exact same-task Repeat Stage that reruns optimization/placements while retaining team/camera; owner live acceptance remains required |
| Expedition reward reroll optimizer | Prototype | Plan tasks select one resource; a compact four-times enlarged OCR pass reads the typed card, application-bundled post-update priors combine with each user's complete local pools and per-device reroll timing, accepted pools continue, and rejected pools use shared source/forward-state retries across verified Start, Settings, Restart/confirm, and operation-scoped cached Expedition Map transitions until accepted or canceled. Difficulty 1/2 priors are extrapolated, thresholds remain provisional, and broader post-update OCR validation is required |
| Event runner | Prototype | Main Macro, Runtime Lab Wire, and managed sessions support Villain Invasion Acts 1-4 with OCR-owned event/act/stage selection, authored per-act placement maps, reusable first-load map preparation, shared placement/terminal handling, and exact same-task Repeat Stage that retains camera/player position. Boss Bounty and Guess That Unit remain Debug-only; owner live acceptance is required |
| Tower runner | Unresolved | The updated Play UI dataset supports detecting and selecting Tower in Runtime Lab Debug, but no map, match, placement, or terminal flow has been specified |
| Placement playback | Prototype | Setup, Runtime Lab, and the main Macro consume authored timelines with quick-place batching; stable phantom panels may receive targeting, Auto Upgrade priority, reconfigure, or sell actions, while upgrade requires physical DPS proof. Place configuration, Reconfigure, and Upgrade end with bounded bottom-right panel dismissal and fresh hidden-panel proof; Sell verifies closure itself. The Start Game boundary finishes prestart actions and requires a fresh verified click before continuing after-start; Runtime Lab and the main Macro additionally own terminal handling, and owner live validation is still required |
| Personalized image-detection runtime | Planned | Decode selected datasets, OCR-bootstrap per-user profiles, run detector-first steady state, and use OCR fallback/refresh; Core fingerprinting and matching primitives are already implemented |
| Private-server links and webhooks | Prototype | The visible private-server field and masked webhook use DPAPI persistence; private-server protocol launch is bounded and validated, while webhook validation/delivery remains Planned |
| Packaging and releases | Prototype | Versioned local owner-test artifacts, strict public GitHub release metadata/asset/checksum policy, coordinated Program Files update shutdown/relaunch, and signed Inno Setup build/publish paths exist; production certificate ownership, anonymous release availability, and compatibility certification remain unresolved |

## Important non-equivalences

- A Debug click proves a bounded transition can be inspected; it does not implement a mode runner.
- A saved placement timeline proves authoring and validation; only an explicit Setup test, Runtime Lab run, or supported macro runner authorizes playback.
- OCR confidence is recorded evidence; it does not independently authorize unattended input.
- Direct `Repeat Stage` remains Debug evidence when invoked manually; the scheduler also owns it for an exact same-task Story/Raid/Expedition/Event continuation after priority is reevaluated.
- Dataset Builder and Runtime Lab are retained owner tools, not legacy code marked for removal.

See [Macro architecture](MACRO-ARCHITECTURE.md) for the planned runtime and [Game behavior](GAME-BEHAVIOR.md) for current Debug behavior.
